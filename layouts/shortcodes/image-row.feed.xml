{{- $inner := strings.TrimSpace .Inner -}}
{{- range split $inner "\n" -}}
	{{- $line := strings.TrimSpace . -}}
	{{- if $line -}}
		{{- $.Page.RenderString (dict "display" "block") $line -}}
	{{- end -}}
{{- end -}}
